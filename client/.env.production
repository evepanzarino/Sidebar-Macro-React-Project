# API won't work on GitHub Pages without a deployed backend
# For now, Users and Items features will fail on GitHub Pages
# Only the PixelGrid will work (it's client-side only)
REACT_APP_API_URL=https://your-backend-url-here.com/api
